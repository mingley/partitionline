{
  "description": "Unsigned CI produce-ack p99 baseline (linger=0, 100B). Relative gate only — not Lab A, not vs C.",
  "produce_ack_p99_us": 500,
  "notes": "Relative baseline for local/fast runners. GitHub Actions sets LATENCY_LIMIT_US=5000 as an absolute GHA+Docker noise ceiling (see .github/workflows/ci.yml). Tighten after signed Lab A latency process exists. See docs/STATUS.md."
}
